08.01.2012.
	* what to do when the ranges for score matrix overlap - make the user select only nonoverlapping ranges?
	* implement plotting for mean profiles over the ranges
	* implement scoreMatrixList and plotting for score matrix list
	* implement naming of the classes on the plot
	
12.02.2012.
	* does not want to build: No definition was found for superclass "SimpleRleList" in the specification of class "modRleList" - this is without scoreMatrixList
	* NAMESPACE file is not made in a proper way - all functions are exported 

28.02.2012.
  * Now R CMD CHECK returns few warnings, NAMESPACE file is there
	* unit tests are pending
  * documentation of data sets are pending
  * Vignette is pending

29.4.2012

 * what is the difference between annotate.WitFeature, annotate.WithFreatureFlank, annotate.WithGenicParts?
 * add a function to plot enrichments as heatmaps - could use ggplot2
 [+] add a function that takes a granges list and a RleList and automaticly produces a scoreMatrixList
 * add a function that takes a granges of genes and consturcts transcript features
 [+] add gff3ToGRanges function
